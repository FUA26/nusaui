---
title: Date Picker
description: A date picker component with range and presets.
---

<ComponentPreview name="date-picker-demo" />

## About

The `DatePicker` is built using a composition of the `<Popover />` and the `<Calendar />` components.

## Installation

<CodeTabs>

<TabsList variant="underline">
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsPanel value="cli">

```bash
npx shadcn@latest add https://nusa-ui.vercel.app/r/date-picker.json
```

</TabsPanel>

<TabsPanel value='manual'>

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install react-day-picker date-fns @radix-ui/react-popover
```

<Step>Install the Calendar and Popover components.</Step>

```bash
npx shadcn@latest add https://nusa-ui.vercel.app/r/calendar.json
npx shadcn@latest add https://nusa-ui.vercel.app/r/popover.json
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="date-picker" title="components/ui/date-picker.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Usage

```tsx
import { DatePicker, DateRangePicker } from "@/components/ui/date-picker"
```

```tsx
const [date, setDate] = React.useState<Date>()

return <DatePicker date={date} onDateChange={setDate} />
```

## Examples

### Date Picker

<ComponentPreview name="date-picker-demo" />

### Date Range Picker

<ComponentPreview name="date-picker-with-range" />

### Date of Birth

Use dropdown navigation to easily select a birth date with year/month selectors.

<ComponentPreview name="date-picker-dob" />

### With Presets

You can add presets for quick date selection.

<ComponentPreview name="date-picker-with-presets" />

## API Reference

### DatePicker

| Prop           | Type                                | Default         | Description                |
| -------------- | ----------------------------------- | --------------- | -------------------------- |
| `date`         | `Date`                              | -               | The selected date          |
| `onDateChange` | `(date: Date \| undefined) => void` | -               | Callback when date changes |
| `placeholder`  | `string`                            | `"Pick a date"` | Placeholder text           |
| `className`    | `string`                            | -               | Additional CSS classes     |

### DateRangePicker

| Prop                | Type                                      | Default               | Description                 |
| ------------------- | ----------------------------------------- | --------------------- | --------------------------- |
| `dateRange`         | `DateRange`                               | -                     | The selected date range     |
| `onDateRangeChange` | `(range: DateRange \| undefined) => void` | -                     | Callback when range changes |
| `placeholder`       | `string`                                  | `"Pick a date range"` | Placeholder text            |
| `numberOfMonths`    | `number`                                  | `2`                   | Number of months to display |
| `className`         | `string`                                  | -                     | Additional CSS classes      |
