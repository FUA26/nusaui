---
title: Toggle
description: A two-state button for turning an option on or off.

links:
  doc: https://www.radix-ui.com/primitives/docs/components/toggle
  api: https://www.radix-ui.com/primitives/docs/components/toggle#api-reference
---

<ComponentPreview name="toggle-demo" />

## Add to your project

<CodeTabs>

<TabsList variant="underline">
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsPanel value="cli">

```bash
npx shadcn@latest add https://nusa-ui.vercel.app/r/toggle.json
```

</TabsPanel>

<TabsPanel value='manual'>

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ui/react-toggle
```

<Step>Paste the source into the matching file in your app.</Step>

<ComponentSource name="toggle" title="components/ui/toggle.tsx" />

<Step>Adjust the import paths to match your app structure.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Example usage

```tsx
import { Toggle } from "@/components/ui/toggle"
```

```tsx
<Toggle>Toggle</Toggle>
```

## More examples

### Outline

<ComponentPreview name="toggle-outline" />

### With Text

<ComponentPreview name="toggle-with-text" />

### Disabled

<ComponentPreview name="toggle-disabled" />

## Props and parts

### Toggle Props

| Prop              | Type                         | Default     | Description                   |
| ----------------- | ---------------------------- | ----------- | ----------------------------- |
| `variant`         | `"default" \| "outline"`     | `"default"` | The toggle variant            |
| `size`            | `"default" \| "sm" \| "lg"`  | `"default"` | The toggle size               |
| `pressed`         | `boolean`                    | -           | Controlled pressed state      |
| `defaultPressed`  | `boolean`                    | `false`     | Default pressed state         |
| `onPressedChange` | `(pressed: boolean) => void` | -           | Callback when pressed changes |
| `disabled`        | `boolean`                    | `false`     | Disable the toggle            |
