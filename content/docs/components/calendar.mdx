---
title: Calendar
description: A date grid for selection, range picking, and calendar workflows.

links:
  doc: https://daypicker.dev
  api: https://daypicker.dev/docs/api
---

<ComponentPreview name="calendar-demo" previewClassName="h-[380px]" />

## Add to your project

<CodeTabs>

<TabsList variant="underline">
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsPanel value="cli">

```bash
npx shadcn@latest add https://nusa-ui.vercel.app/r/calendar.json
```

</TabsPanel>

<TabsPanel value='manual'>

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install react-day-picker date-fns
```

<Step>Paste the source into the matching file in your app.</Step>

<ComponentSource name="calendar" title="components/ui/calendar.tsx" />

<Step>Adjust the import paths to match your app structure.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Example usage

```tsx
import { Calendar } from "@/components/ui/calendar"
```

```tsx
const [date, setDate] = React.useState<Date | undefined>(new Date())

return (
  <Calendar
    mode="single"
    selected={date}
    onSelect={setDate}
    className="rounded-md border"
  />
)
```

## More examples

### Date Range

You can select a range of dates using the `mode="range"` prop.

<ComponentPreview name="calendar-with-range" previewClassName="h-[420px]" />

### Multiple Months

Display multiple months using the `numberOfMonths` prop.

<ComponentPreview
  name="calendar-multiple-months"
  previewClassName="h-[420px]"
/>

### With Dropdown Navigation

Use dropdowns to navigate between months and years.

<ComponentPreview name="calendar-with-dropdown" previewClassName="h-[380px]" />

## Props and parts

### Calendar

The `Calendar` component is built on top of [React Day Picker](https://daypicker.dev).

| Prop              | Type                                         | Default    | Description                        |
| ----------------- | -------------------------------------------- | ---------- | ---------------------------------- |
| `mode`            | `"single" \| "multiple" \| "range"`          | `"single"` | Selection mode                     |
| `selected`        | `Date \| Date[] \| DateRange`                | -          | The selected date(s)               |
| `onSelect`        | `(date: Date \| undefined) => void`          | -          | Callback when date is selected     |
| `disabled`        | `Date[] \| (date: Date) => boolean`          | -          | Dates to disable                   |
| `numberOfMonths`  | `number`                                     | `1`        | Number of months to display        |
| `showOutsideDays` | `boolean`                                    | `true`     | Show days from adjacent months     |
| `captionLayout`   | `"label" \| "dropdown" \| "dropdown-months"` | `"label"`  | Layout of the month caption        |
| `fromYear`        | `number`                                     | -          | Start year for dropdown navigation |
| `toYear`          | `number`                                     | -          | End year for dropdown navigation   |
| `defaultMonth`    | `Date`                                       | -          | The initial month to display       |
