---
title: Spinner
description: A lightweight loading indicator for asynchronous work.
---

<ComponentPreview name="spinner-demo" />

## Add to your project

<CodeTabs>

<TabsList variant="underline">
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsPanel value="cli">

```bash
npx shadcn@latest add https://nusa-ui.vercel.app/r/spinner.json
```

</TabsPanel>

<TabsPanel value='manual'>

<Steps>

<Step>Paste the source into the matching file in your app.</Step>

<ComponentSource name="spinner" title="components/ui/spinner.tsx" />

<Step>Adjust the import paths to match your app structure.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Example usage

```tsx
import { Spinner } from "@/components/ui/spinner"
```

```tsx
<Spinner />
```

## More examples

### Sizes

Use the `className` prop to adjust the size.

<ComponentPreview name="spinner-sizes" />

### Button with Loading State

<ComponentPreview name="spinner-button" />

## Props and parts

### Spinner

| Prop         | Type     | Default     | Description            |
| ------------ | -------- | ----------- | ---------------------- |
| `className`  | `string` | -           | Additional CSS classes |
| `aria-label` | `string` | `"Loading"` | Accessible label       |

All standard SVG props are also supported.

## Accessibility notes

- Uses `role="status"` for screen readers
- Includes `aria-label="Loading"` for context
