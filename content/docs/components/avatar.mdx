---
title: Avatar
description: A compact identity visual with image and fallback support.

links:
  doc: https://www.radix-ui.com/primitives/docs/components/avatar
  api: https://www.radix-ui.com/primitives/docs/components/avatar#api-reference
---

<ComponentPreview name="avatar-demo" />

## Add to your project

<CodeTabs>

<TabsList variant="underline">
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsPanel value="cli">

```bash
npx shadcn@latest add https://nusa-ui.vercel.app/r/avatar.json
```

</TabsPanel>

<TabsPanel value='manual'>

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ui/react-avatar
```

<Step>Paste the source into the matching file in your app.</Step>

<ComponentSource name="avatar" title="components/ui/avatar.tsx" />

<Step>Adjust the import paths to match your app structure.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Example usage

```tsx
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar"
```

```tsx
<Avatar>
  <AvatarImage src="https://github.com/shadcn.png" alt="@shadcn" />
  <AvatarFallback>CN</AvatarFallback>
</Avatar>
```

## More examples

### With Fallback

<ComponentPreview name="avatar-fallback" />

### Different Sizes

<ComponentPreview name="avatar-sizes" />

## Props and parts

### Avatar

| Component        | Description                       |
| ---------------- | --------------------------------- |
| `Avatar`         | The root container                |
| `AvatarImage`    | The image element                 |
| `AvatarFallback` | Fallback when image fails to load |

### AvatarImage Props

| Prop  | Type     | Default | Description            |
| ----- | -------- | ------- | ---------------------- |
| `src` | `string` | -       | The image source URL   |
| `alt` | `string` | -       | Alt text for the image |

### AvatarFallback Props

| Prop      | Type     | Default | Description                   |
| --------- | -------- | ------- | ----------------------------- |
| `delayMs` | `number` | -       | Delay before showing fallback |
